import {StyleSheet} from 'react-native'


const styles = StyleSheet.create({
    container: {
        marginTop: -50,
        height: 300,
        width: '100%', 
        backgroundColor: '#fff' ,
        elevation: 3,
        borderRadius: 8,
    }
})

export default styles